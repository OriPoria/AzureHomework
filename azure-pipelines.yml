# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  name: default

steps:
- script: |
    az group create --name orires2 --location eastus
    az deployment group create --resource-group orires2 --template-file res.json --parameters res.parameters.json
  displayName: 'deploy storage account and server'
